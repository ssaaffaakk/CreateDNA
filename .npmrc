# Render (and most PaaS hosts) set NODE_ENV=production, which makes npm omit
# devDependencies — but the build needs them: @tailwindcss/postcss powers
# globals.css and `next build` type-checks with typescript.
# Pinning include=dev here keeps the build working no matter which install
# command the host runs.
include=dev
